meta {
  name: Logout
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/logout
  body: none
  auth: inherit
}

script:post-response {
  const data = res.getBody();
  bru.setEnvVar("apiToken", data.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
