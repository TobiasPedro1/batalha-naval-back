meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "teste01",
    "password": "1234567890"
  }
}

script:post-response {
  const data = res.getBody();
  bru.setEnvVar("apiToken", data.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
