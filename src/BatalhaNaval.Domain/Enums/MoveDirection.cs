namespace BatalhaNaval.Domain.Enums;

public enum MoveDirection
{
    North, // Cima (Y diminuindo)
    South, // Baixo (Y aumentando)
    East, // Direita (X aumentando)
    West // Esquerda (X diminuindo)
}